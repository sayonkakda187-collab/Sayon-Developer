node_modules/
# Persistent browser profile = your live Facebook session. NEVER commit it.
profile/
# Exported session file (storageState) = a live credential. NEVER commit it.
session.json
# Local env with your real token. Commit only .env.example.
.env
.env.local
